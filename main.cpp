#include "bars.h"

int main() {
    Bars bars;
    //bars.performSort(BUBBLE);
    //bars.performSort(INSERTION);
    //bars.performSort(SELECTION);
    bars.performSort(QUICK);
    bars.performSort(MERGE);
    //bars.performSort(GNOME);
    //bars.performSort(STOOGE);
    //bars.performSort(BOGO);

    return 0;
}